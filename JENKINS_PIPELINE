pipeline {
  agent any
    
  tools {nodejs "NodeJs"}
    
  stages {
            
    stage('Build') {
      steps {
        bat 'npm install'
        bat 'npm install forever -g'

      }
    }  

    stage('Deliver') {
      steps {
        bat 'forever start server.js'
        bat 'forever list'
      }
  }
           
  }
}